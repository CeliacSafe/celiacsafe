/**
 * Admin-Zugang (Client-only, kein Backend).
 * PIN vor Release ändern — nur für interne Admin-Builds / Curator-Geräte.
 */
export const ADMIN_PIN = '4829';

/** 5× Tippen auf Versionszeile im Profil öffnet Admin-Login */
export const ADMIN_UNLOCK_TAPS = 5;
